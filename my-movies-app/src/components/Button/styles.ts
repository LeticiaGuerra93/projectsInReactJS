import styled from "styled-components";

export const ButtonBase = styled.button`
  padding: 0.5em;
  margin: 0.5em;
  color: var(--black);
  background: var(--white);
  border: none;
  border-radius: 5px;
  width: 100px;
  display: flex;
  align-items: center;
`;
