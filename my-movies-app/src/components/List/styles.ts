import styled from "styled-components";

export const ListBase = styled.ul`
  color: var(--black);
  list-style: none;

  display: flex;
  justify-content: center;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 0;
`;
